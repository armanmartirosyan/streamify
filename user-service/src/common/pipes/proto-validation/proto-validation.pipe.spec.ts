import { ProtoValidationPipe } from './proto-validation.pipe';

describe('ProtoValidationPipe', () => {
  it('should be defined', () => {
    expect(new ProtoValidationPipe()).toBeDefined();
  });
});
